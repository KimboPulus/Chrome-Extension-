{
  "name": "focus-meter-extension",
  "version": "1.0.0",
  "private": true,
  "description": "A local-first Chrome extension for tracking active browsing time.",
  "scripts": {
    "check": "node --check background.js && node --check content/activity.js && node --check popup/popup.js && node --check options/options.js && node --check blocked/blocked.js",
    "test": "node --test"
  }
}

